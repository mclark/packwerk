# This file contains a list of dependencies that are not part of the long term plan for components/timeline.
# We should generally work to reduce this list, but not at the expense of actually getting work done.
#
# You can regenerate this file using the following command:
#
# bundle exec packwerk update-deprecations components/timeline
---
components/sales:
  "::Order":
    violations:
    - privacy
    files:
    - components/timeline/test/private_thing_test.rb
